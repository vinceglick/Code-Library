#Create a matrix of two vectors where v_1 is the consolidated identifier
#for those values in v_2 (i.e. row names for column values, 4x2 is used as an example)
A <- matrix(data=c(v_1,v_2), nrow=4, ncol=2)