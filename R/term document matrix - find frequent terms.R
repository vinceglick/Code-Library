#The most frequent terms are derived from each vector source, 
#which one can intuitively utilize to gain a perspective on what 
#roots will have the greatest scope for each vector source 

findFreqTerms(x_tdm, 50000)